# Habit Tracker Web

This repository contains a React + Vite habit tracker with:

- Local storage of habits
- Import / export (JSON)
- Reminders while app is open
- Service Worker + Push subscription client
- Server example (Node + Express + web-push) to send push messages

See README in /server for server instructions and VAPID key setup.
